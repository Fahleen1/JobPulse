/**
 * Placeholder entry for `npm run proof`.
 * Module 1 Parts 3–5 will replace this with the live Ashby proof CLI.
 */
console.log(
  "JobPulse proof CLI scaffold ready. Parts 3–5 will fetch and classify Ashby jobs.",
);
